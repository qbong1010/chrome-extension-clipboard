Task 6 evidence: API key hardening and permission stance

Code changes:
- `storage-utils.js` now includes `buildingApiKey` in `DEFAULT_SETTINGS` and settings normalization.
- `sidepanel.html` now exposes `#buildingApiKeyInput` and `#saveBuildingApiKeyBtn` in Settings.
- `sidepanel.js` now reads the API key from `extensionSettings.buildingApiKey` and throws `API 키를 먼저 설정해주세요` before any request when the key is absent.
- `tests/settings-storage.test.js` covers save + clear behavior for `buildingApiKey`.
- `tests/e2e/sidepanel-code-lookup.spec.js` covers the missing-key UI path.

Validation:
- `npm test` -> PASS (15/15)
- `npx playwright test tests/e2e/sidepanel-code-lookup.spec.js --project=chromium` -> PASS (5/5)
- `npm run package:extension` -> PASS
- Secret scan for the previous literal API key and `const API_KEY` in tracked source/docs -> PASS (no matches)

Permission decision:
- `host_permissions: <all_urls>` remains unchanged in source manifest for this cycle.
- Rationale is documented in `docs/RELEASE_CHECKLIST.md` pending a dedicated content-script permission audit.
